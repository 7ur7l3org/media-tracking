thanks https://chatgpt.com/c/679d3064-16b0-800c-9e35-89509164697b (trying o3-mini)
thanks https://chatgpt.com/c/679d8f5e-ad78-800c-ac21-cba9c0ffbd01 (o3-mini is good!)
thanks https://chatgpt.com/c/67d26a50-554c-800c-87ae-b8cb1d2f8cce
thanks https://chatgpt.com/c/67d7c151-3f90-800c-9811-cf45a2920d74 (cloudflare workers cors proxy deployment to https://ueue-cors-proxy.7ur.workers.dev)
thanks https://chatgpt.com/c/67e1015d-38c4-800c-9eaf-ac5ea8796807 (cloudflare pages site deployment to https://ueue.pages.dev/)

can use something like https://ueue.7ur.it/?id=Q5884621 for unknown uncategorized everything
can enter note saying ep3 for stuff that doesn't have episodes yet

phase 0
- TODO move things here. phase 1 got fucked

phase 1
- 22ee09bd23e5e62da28130b6ba8183d3ca7f5dd8 is a bad commit that reverted the change that another had committed :/ shouldn't be possible cause should at least merge conflict but i guess sharing the repo makes it fucked. writing the file is not safe if the file has changed (by another tab) ffs
- broken site. i inspected the tab from chrome://inspect/#devices and it randomly started working. but i caught these errors:
    GitSyncStatus: syncing repo
    backend.js:19 ueue-media-tracking.json not found – initializing new file.
    loadBackendData @ backend.js:19Understand this warning
    main.js:7 Sync failed: Error: Mutex timeout
        at @isomorphic-git_lightning-fs@4.6.1_dist_lightning-fs.min.js:1:11786
    (anonymous) @ main.js:7Understand this error
    @isomorphic-git_lightning-fs@4.6.1_dist_lightning-fs.min.js:1 Uncaught (in promise) AbortError: signal is aborted without reason
        at @isomorphic-git_lightning-fs@4.6.1_dist_lightning-fs.min.js:1:11776Understand this error
    3@isomorphic-git_lightning-fs@4.6.1_dist_lightning-fs.min.js:1 Uncaught (in promise) Error: Mutex timeout
        at @isomorphic-git_lightning-fs@4.6.1_dist_lightning-fs.min.js:1:11786
    then it magically started working after refresh...
        [1:44:32 PM mistress f0f3439] syncing repo
        [1:44:32 PM mistress f0f3439] committing changes: Auto-commit pre-sync
        [1:44:32 PM mistress 5a45fc3] committed changes: Auto-commit pre-sync
        [1:44:32 PM mistress 5a45fc3] local repo exists – fetching updates...
        [1:44:33 PM mistress 5a45fc3] repo fetched successfully
        [1:44:33 PM mistress 5a45fc3] local HEAD (5a45fc3) has extra commits
        [1:44:33 PM mistress 5a45fc3] comparing local HEAD (5a45fc3) to fetched origin/mistress (11fa5a3)
        [1:44:34 PM mistress 5a45fc3] local HEAD (5a45fc3) has extra commits
        [1:44:34 PM mistress 5a45fc3] pushing changes to GitHub...
        [1:44:34 PM mistress 5a45fc3] pushOrDivergeAndPush: Push rejected due to non-fast-forward. Creating session branch...
        [1:44:34 PM mistress 5a45fc3] pushOrDivergeAndPush: Creating and switching to branch: mistress-session-1752525874964
        [1:44:34 PM mistress-session-1752525874964 5a45fc3] pushOrDivergeAndPush: Switched to branch: mistress-session-1752525874964
        [1:44:41 PM mistress-session-1752525874964 5a45fc3] pushOrDivergeAndPush: Sync complete: diverged changes are now pushed to branch mistress-session-1752525874964.
        [1:44:42 PM mistress-session-1752525874964 5a45fc3] Creating PR for branch mistress-session-1752525874964...
        [1:44:42 PM mistress-session-1752525874964 5a45fc3] Creating PR for branch mistress-session-1752525874964...
        [1:44:42 PM mistress-session-1752525874964 5a45fc3] PR already exists for branch mistress-session-1752525874964. Trying again to retrieve it.
        [1:44:43 PM mistress-session-1752525874964 5a45fc3] PR created for branch mistress-session-1752525874964.
- when on a diverted branch it attempts a push because it compares to the wrong branch head (fine ish cause the push fails)..
    [1:48:33 PM mistress-session-1752525874964 38617c4] local repo exists – fetching updates...
    [1:48:34 PM mistress-session-1752525874964 38617c4] repo fetched successfully
    [1:48:34 PM mistress-session-1752525874964 38617c4] local mistress-session-1752525874964 already matches fetched origin/mistress-session-1752525874964
    [1:48:34 PM mistress-session-1752525874964 38617c4] comparing local HEAD (38617c4) to fetched origin/mistress (11fa5a3)
    [1:48:35 PM mistress-session-1752525874964 38617c4] local HEAD (38617c4) has extra commits
    [1:48:35 PM mistress-session-1752525874964 38617c4] remote already updated (skipping push)
- maybe make clicking links not always open in new tabs
- add `My Dress-Up Darling` to search tests
- add actual descriptions for things with imdb or tvdb id's where possible
- add monogatari and monagatari to search test cases
- add the handmaid's tale to ueue search tests that have crazy results
- make searches write to tab history and url and make back and forward go between them
- queue vote manual resolve tickbox and show votes and consumptions and notes and toggle to filter showing resolved vs unresolved. maybe tags are the way smfh
- add accessibility key web thing to jump to search input
- consider partial expanding properties sections to the first one or two.. might be ugly tho
- could possibly make primary token create submodule repos that are more restrictively scoped and tag queues with that sub-repo and have things be mirrored (or explicitly maintained) in those repos, then visibility of those repos can be controlled and opened up / shared with people. e.g. goal is "make equeue public/shareable with a link" by either authing on github and having the login ux have her log in with github to the repo with a scoped token (somehow..) or just by sharing the more restrictive token in the url
- add search for "i am sam" to test cases. super slow and bad
- get duration on the queue votes page
- make queue votes column have a copy button to copy as list
- fix https://ueue.7ur.it/?id=Q131534559 my hero academia vigilantes being completely broken somehow
- add `https://ueue.7ur.it/?id=Q63615353 Seishun Buta Yarō - Japanese anime television series` text to page to more easily copy url and description (would be slightly better than the url hint in general for copy pasting to e.g. here)
- make queues page be tick box for which queues to show by url and update page title
- make https://ueue.7ur.it/?id=Q823422 show back parts like https://ueue.7ur.it/?id=Q189505 does. make the latter show jaws 2 outside of those back link siblings
- update all formatter url P1630 uses to lowkey dodge https://toolhub.wikimedia.org/tools/toolforge-wikidata-externalid-url by preferring `applies if regular expression matches (P8460)` formatter urls. should always use/provide all formatter urls that match but sorted by explicit match first and then alt urls are provided with maybe [1] or [2]
- what in the world bugged series/sequencing https://ueue.7ur.it/?id=Q22712210
- make series/sequencing show descriptions in addition to titles in collapsible nodes e.g. https://ueue.7ur.it/?id=Q14566553 just shows three different qid's with the same name
- make url tilde metadata thing
- make page load fast probably by caching wikidata data in repo and only updating that after first render with loading icon..
- make search use google api if given (just add to indexeddb for now or maybe <account-settings-repo>/account-settings-secrets.json)
- make search use other better search backends with buttons <load google search (default), load mediawiki search, load blah search, google did you mean / completions> where they cascade if no or few hits for the first ones
- address crazy-behavior.txt
- make private repo hold secrets and settings such as other github tokens for other repos and google search api/site and cors proxy and stuff. also holds extensions and such. or has token for public repo that holds extensions. then login is pure token/ueue-account-settings-repo
- make search better. seinfelt should work even if smol typo
- search is now the slow part of the site. this takes 12 seconds `SELECT ?itemId WHERE {      SERVICE wikibase:mwapi {        bd:serviceParam wikibase:api "Search" .        bd:serviceParam wikibase:endpoint "www.wikidata.org" .        bd:serviceParam mwapi:srsearch "friends" .        bd:serviceParam mwapi:srlimit "20" .        bd:serviceParam mwapi:sroffset "0" .        ?itemId wikibase:apiOutput mwapi:title .      }    }    LIMIT 20    OFFSET 0`
- add profiling to every function and show it at the bottom near the git operations log. should be easy to figure out what's slow
- ability to move/send consumptions or queue votes to different (q)id's
- when there arent episode information and i am adding consumption s01e12 it should autosuggest s01e13 as the next consumption 
- change it so that instead of focusing the search, the search is a single tab away
- expose all useful js functions that give data under /api?blah that return the actual json that the site uses to work out the stuff. maybe even make the site itself use that if it isn't too slow so the network tab looks good. probably allow https auth tokens in query parameters (look up safety of that) so other tools can easily grab equivalent stuff that ueue does. same for ui components under /components. they should all be able to work fine if given the right context parameters
- theoretically private git repo should be able to hold additional js api and html components that can be composed on the site. like extensions/plugins. not sure how to load web stuff from indexeddb files but if i make some kinda /local-repo i think i could just hit those for html/js
- make /local-repo that shows http folder structure and all files in repo. branch query parameter and ui dropdown
- make series/sequencing rendering more reflective of the actual data being rendered e.g. flat-collection reverse links (P?) vs P527* hierarchies. e.g. P179 is probably just a lie at this point
- move follows / followed by out of the collapsible series. those statements are universal, not specific to any series
- add ancestry rendering alongside follows / followed by. add P?'s to ancestry/follows/followed by and probably always render it even if those statements don't exist. (no statement) should be good to see for those three navigational claims and more uniform rendering is better
- add follows / followed by / ancestry to the top in addition to the bottom of the series/sequencing
- add arrow rendering to flat-collection series rendering for which one we're on
- add qid rendering to flat-collection entries (e.g. series/sequencing of ?id=Q208269) and sqid/wikidata to all series/sequencing
- `if (instanceIds.includes("Q7725310") || instanceIds.includes("Q24856")) { // TODO what the fuck is this` figure out what the fuck this is in rendering.js
- test that multiple series/sequencing still work and add to test cases at the bottom of the partonomy prototype code
- add smol "copy" buttons to all property values (and maybe everything everywhere lol)
- add link to the good wikidata search in the search results pane (even better if it retains the failed query). likewise for wikidata google search minimally to grab some sort of uri for the id. even better maybe to get the closest match on wikidata e.g. the author or something maybe via google? well or just best to have form to more easily make the thing
- implicit resolution of queue votes and explicit cause i dont want to deal with aggregation implicit yet
- make it default to a date inout field of current time (e.g. page load time) that is editable but with a now button to set it to now for all consumptions
- integrate optimized partonomy for series/sequencing. thanks https://chatgpt.com/c/682d1e01-eef8-800c-8a00-a12cf11d2eca holy shit this is way better
- integrate external url resolver via P8966 url patterns https://chatgpt.com/c/680f9094-e8a8-800c-91e1-28bd23e1afa5 resolveWikidataEntityFromUrl
- import trakt. it's right there. just clean up the one thing
- add common templating thing to pages, that only shows if it hits, and for art probably. https://chatgpt.com/c/682bcd20-ec2c-800c-bdf2-83de6b27f8c4
- make the queues each have a colour and highlight differently
- make sparql json api stuff on site maybe as data explorer page or about page linking to all sparql queries used
- make search result hover and content pages show cover picture of thing
- add persistent https://tvmaze-to-ical.7ur.workers.dev/ pattern link whenever tvmaze or imdb or thetvdb id's are available to add dynamic ics feed of show broadcasts to calendars
- make consumptions and queue votes show notes
- make queue vote resolution (possibly default to ordering where consumptions resolve prior votes and then future votes count as votes.. implicit resolve. but probably do need some kind of explicit resolve for series completion idk esp if i'm abusing consumptions of the main show when episodes are missing but i need to retain the queue vote. actually yeah for now definitely just add queue vote resolution)
- make title and url (and url either updating fast or get it in the link from the series/sequencing. probably the former)
- add ics broadcasting information ics calendar dynamic url feeds to wikidata pages. those are super useful. I'm sure some content things track that stuff already. https://chatgpt.com/c/68229d11-08a4-800c-9ca7-23a432aaa16b
- add about page with deeper explanation and somehow embed current git sha in the site somewhere automatically on commit maybe unless it's weird that being a commit behind or something
- make consume on the last episode of a season auto-epxand the next season
- make most recent consumptions on the top and the add consumption at the top too
- make queue table have the queue votes listed inline as a column so you can edit which queue a vote is on
- implement list resolution and public lists!
- make global notes that are rendered on every page in addition to per content. make them support property templates the same as wikidata url templates, so e.g. the tmdb id and series episode ordinals can directly populate links and such
- add https://myanimelist.net/anime.php?id=48736 style url match pattern to https://www.wikidata.org/wiki/Property:P4086 (when i have enough cred? seems like https://www.wikidata.org/wiki/Wikidata:Requests_for_comment/General_semi_protection_for_all_property_pages it's semi protected and i just need 50 edits https://www.wikidata.org/wiki/Wikidata:Autoconfirmed_users lmao)
- make https://ueue.7ur.it/?id=Q558112 fucking show up in the search results what in the world
- actually remove all sha routing and api style urls. i'm too trakt pilled. google has used query parameters for everything forever and they're fine. id=blah is the most generic anyway if my backend data only has one id key. we want something that looks like https://ueue.7ur.it/?id=Q114481439&~=~(young-sheldon~s1e7~2017~a-brisket-voodoo-and-cannonball-run~episode-of-young-sheldon-s1-e7)~#seriesContainer
- call e.g. document.getElementById(location.hash.slice(1))?.scrollIntoView(); after (all) content loads so that it scrolls to the right place
- make token mostly not visible unless you click
- add whatever type this is to list of search result emphasises https://ueue.7ur.it/?id=Q99955255 . maybe grab the whole type hierarchy at some point and see what it looks like
- make separate file that is media information cache that holds all wikidata response information for all the things you've ever visited, but that doesn't as obviously get mixed in with your actual data. alternatively for now rip out all existing "visited" pages from the json since they are pretty worthless (like what you hadn't yet decided to put on watchlist and you remember when you lose internet? lol)
- condense json
- add a resolve to queue votes so they stay but just are closed, instead of deleting them. should never have to remove a queue vote when watched, you'd want the history of the votes and the lag in watching. should show in the page info, and not on the queue votes page
- update grab-updated-prompt.py to exclude site/external (or tree-sitter or something them into what api is available); try aider copy paste mode with gemini https://aistudio.google.com/app/prompts/new_chat?model=gemini-2.5-pro-exp-03-25 , 
- turn current query parameter url format of index.html into api-style sha routing with /qid/<qid>(=title~desc) human eye metadata in the url for betteer ctrl+shift+a etc (initially before full api routing) e.g. `https://ueue.7ur.it/#/qid/Q114481439(=a-brisket-voodoo-and-cannonball-run~episode-of-young-sheldon-s1-e7)`. also /#/search?q=blah . eventually /qid/<qid>(=[s3e7|2014]title~desc) depending on if we can extract the season/episode number or year from the description else series/sequencing
- auto resolve all queue votes on consume
- eventually can suggest queue votes to resolve from series sequencing completion
- make queue vote page for all content segregated into a tree by wikidata media type, then i dont need to manually track books and movies separately etc (then queue is more like just timestamped tags for filtering)
- make page title pluck out sNeM if it can from the description or series/sequencing, in addition to year at the end and year at the start of the slug
- make there be a sync at the end of all content load. i think that's what we're missing cause the data changes at the end of the content load or some shit
- make series/sequencing show and sum duration
- opened two tabs at once. looks like this one lost the push race? refresh was totally happy. weird
    - [9:19:44 PM mistress 707f302] syncing repo
    - [9:19:44 PM mistress 707f302] committing changes: Auto-commit pre-sync
    - [9:19:44 PM mistress 89161a7] committed changes: Auto-commit pre-sync
    - [9:19:44 PM mistress 89161a7] local repo exists – fetching updates...
    - [9:19:46 PM mistress 89161a7] repo fetched successfully
    - [9:19:46 PM mistress 89161a7] local HEAD (89161a7) has extra commits
    - [9:19:46 PM mistress 89161a7] comparing local HEAD (89161a7) to fetched origin/mistress (707f302)
    - [9:19:46 PM mistress 89161a7] local HEAD (89161a7) has extra commits
    - [9:19:46 PM mistress 89161a7] pushing changes to GitHub...
    - [9:19:49 PM mistress 3773f81] pushOrDivergeAndPush: Error pushing changes: One or more branches were not updated: 
    -   - refs/heads/mistress: cannot lock ref 'refs/heads/mistress': is at 3773f81affd65cd687019f35d61c480c0ef99a82 but expected 707f302d4d800921ae8f5057a2de9acee4a32ad6
- bro wtf are the search results for faouzia. google hits immediately. wtf
- searching my hero academia vigilantes shows `My Hero Academia: Vigilantes (Q131534559) [sqid] [wikidata] – [anime television series]` looking jacked compared to e.g. `My Hero Academia: Vigilantes (Q106091435) [sqid] [wikidata] – Japanese manga series [manga series, spin-off]`
- make queues page filter work and show queue vote notes and content notes
- make edit buttons for stuff
- eventually make site work over file:// with hash routing as an alternative to spa routes
- make direct buttons to add queue votes to each queue without having to dropdown e.g. the queue button submits to that queue
- https://ueue.7ur.it/?id=Q105105219 lmao series/sequencing isn't merged somehow
- persistent dynamic notes for all pages from private repo that links to e.g. s1e1 via tmdb id of the streaming sites
- make load by external url or generic external id esp if unique, else search. both on page in search and in url
- add publication date and start time and end time to table of all things in json. in fact add table query page where you can add whatever columns you wanna see across all media. matrix view of all creative works properties and custom other properties
- if publication date and start time and end time are reliable get those in the url slug and in the search results and in series sequencing view
- dynamically update urls with human readable part https://ueue.7ur.it/qid/Q550555(gravity-falls~american-animated-television-series) or https://ueue.7ur.it/qid/Q114481439(=a-brisket-voodoo-and-cannonball-run~episode-of-young-sheldon-s1-e7)?type=show&sort=desc#results
- merge remote into here i know i updated it on github
- merge master into mistress. see if there's any logs for how that happened where trash internet got the repo re-initialized from nothing
- make all queues show single table with all entries where one column is the queue but its all sorted in reverse chronological order
- put :hyphenated-title-description-slug in wikidata qid urls https://chatgpt.com/c/67f7203c-1730-800c-b1be-9e840d2c7807
- minify json to not have empty fields also put jsonp schema in the meta. then use .? everywhere
- share links via url read only token that hijacks the site ui ig
- eli says shared repo lists are desired. maybe each list is tied to a repo and the single token is granted perms to push to all repos? or share edit tokens? user data repo? overlay views that merge multiple with view only pats? actually probably each user's main repo is private and just holds all repos and pats that they have access to and then the client uses those pats to write to those repo/lists
- pwa https://chatgpt.com/c/67f6a7c1-dc34-800c-81dd-0e781c9b2f1c
- also deploy to github pages now that the repo is public. static site mirrors should work completely fine
- set default branch to mattress so i dont keep committing readme changes to mistress...
- why the fuck is css broken -> deploy turns <link rel="stylesheet" href="styles.123abcd.css"> into <link rel="styles.dca3bc1.css"> amazing
- probably half revert deploy back into just sticking ?v=N into all references in the htmls. should be able to navigate to the files by name. having the filenames change is cancerous and i don't really want even more commits to make it look and feel normal on dev
- make sitemap or filemap or some index of the site's files somehow
- how the fuck did i diverge onto master from trash network. master has no history in common with mistress so failed to make pr. what in the world. did its indexeddb get wiped?
    - it came from an auto commit pre-sync. somehow the files existed but we were still on master.. and then did something to force a push.. which probably kept us diverged. does the clone/fetch not always put us on mistress? how could we have files without being on a legit commit? i don't think we ever git init..
    - post-merge of master into mistress. what in the fucking world. it did put my phone chrome back on mistress
        [3:59:53 AM] syncing repo
        [3:59:53 AM] committing changes: Auto-commit pre-sync
        [3:59:53 AM] Error committing changes: Could not find HEAD.
        [3:59:53 AM] local repo exists – fetching updates...
        [3:59:53 AM] cloning repo...
        [3:59:54 AM mistress 9e09b28] repo cloned successfully
        [3:59:54 AM mistress 9e09b28] remote already updated (skipping push)
    - renamed master to master--TODO-DEBUG-HOW-THIS-HAPPENED on gitlab for future investigation
    - possibly sync git operations log in every commit message if pushing to non-mistress?
- sync on visible, content notes, edits, queue ordering and filtering, pwa
- make sure json always sorts all entries by key. merges should be better that way. it might just be append to the end currently which encourages diverges to conflict
- make main ueue clickable
- when very bad internet it hangs on fetch. should prompt to go offline or whatever (well it should just work fine and not hang... sync stuff is like.. background, right?) and should load
- when airplane mode the site does the dinosaur game. time to pwa or otherwise make it always able to work
- make sitemap or filemap or some index of the site's files somehow
- make series/sequencing etc load separately
- probably half revert deploy back into just sticking ?v=N into all references in the htmls. should be able to navigate to the files by name. having the filenames change is cancerous and i don't really want even more commits to make it look and feel normal on dev
- add consumption with note inline in series/sequencing view on button
- make series/sequencing cache common per root qid across all content pages of that series
- make cache forever and only update in background ("verifying..."). possibly put in heavier cache.json instead of manual indexeddb tbh (device should always have a clone so it should only be the background verify to fetch re-query and update whenever, but otherwise should be used as existing indexeddb is)
- fix visiting content not pushing the commit leading to almost inevitable diverge on refresh (also doesn't make any sense indexeddb changing in the background but the commit living... somewhere??? well maybe the diverge is purely from two devices which is fair)
- make wikidata cache live forever until manual refresh (if success cause internet). few things aren't well in the past
- fix queues and consumptions pages (queues?queue=books doesn't only load books. consumptions seems to work. both are missing notes and number of votes and probably sorting)
- add edit buttons to notes and timestamps for queue votes and consumptions
- make queues page allow reordering queues for the add new queue vote ui, and deleting queues (and optionally purging all queue votes of that deleted queue from all media)
- make external non-qid id pages for failed searches or uri's that should still function fine as id's for consumption/queue ui, probably just button on search failure and automatic if search failure and no hits
- make proper pwa that works offline with indexeddb storage
- make the ui not flicker - merge the new dom into the old dom directly updating contents and only inserting/deleting elements when necessary
- make consumptions and queue votes show loading/progress in the new entry as it is syncing to github
- make notes ui per id
- dark mode by system default and toggle in corner (https://chatgpt.com/c/67e24631-3cb4-800c-8c74-b2cffff5cb09 js in head to set theme from user toggle localStorage else os preference, use variables for everything, no themes)
- fix login button to always prompt to update/add username/password if changed somehow in chrome password manager
- ensure repo caches are wiped on logout and login -> orrr just the tokens so you can "login" to different offline repos? or maybe ux for "local repos" where you can wipe them if you want idk. like if you log out and then you see all the local repos you could log into without the pat or you could wipe them cause you see that they're there. that sounds like the best
- make series/sequencing show top level consumed and queue votes for e.g. House in the Part of the series line just like e.g. season 5 does
- integrate wikidata images or wikipedia or mpdb or tmdb images or fanart or otherwise to get images on content
- add ` Read access to metadata; Read and Write access to code and pull requests` to list of what the token should have in login section
- put meta title/description in commit messages
- make cache series/sequencing for all elements of the series if it doesn't already
- make title/description include type of tags like the search shows
- make search work offline to search through all the things you've ever visited
- make adding consumption default to removing from watchlist probably? tho that might just be nice to do from its own page later idk
- visiting a page for the first time commits the title/description to the repo, but only locally. it doesn't trigger a sync. then when you refresh and visit it for the first time you get a "auto-commit pre-sync" on initial sync because the local repo is ahead. the local repo should never be ahead because all commits should be "then sync" triggering actions
- the edit of votes should be able to switch which queue an entry is on directly
- when logged out i get `pushOrDivergeAndPush: Error: Please log in with your GitHub token first.` red at the top, but not in the sync status where it should clearly show "local only". the logs are `[11:04:20 PM mistress fe363a1] syncing repo  [11:04:20 PM mistress fe363a1] no changes to commit  [11:04:20 PM mistress fe363a1] cloneOrFetchUpdates: Error: Please log in with your GitHub token first.  [11:04:20 PM mistress fe363a1] pushOrDivergeAndPush: Error: Please log in with your GitHub token first.`. in fact when logged out and on the home page it's only a single yellow warning saying "syncing repo" since i guess the local repo wasn't even initialized yet (it becomes the same red once you visit any content page)
- logging in doesn't trigger a sync until refresh
- make it way easier to share login password/username between devices/sites, copy pasting the token and cors proxy separately and # appending them is dumb. should probably just show username and password directly possibly idk
- make slow generic "loading..." actually incrementally load or something or at least show clearly what it's loading (e.g. main part first then series/sequencing is loading)
- cache series/sequencing information in the ueue-media-tracking.json directly (similarly to caching visited content) with just ui to refresh/reload the series/sequencing information for a piece of content. probably this would be in .meta.series where all the root series are placed there. should make the wikidata cache less important too which is good
- "http is not defined" is what flashes red in the background of the sync status. i think when that happens the sync actually did completely fail but for some reason the site thinks it worked.. eventually refreshing enough it starts taking a second to actually fetch. maybe it takes a bit for the cloudflare worker to kick on? it should be very unhappy in that state.. i think the scripts aren't always loading in the right order with the right dependencies or something
    - ooh i think i caught it: `pushOrDivergeAndPush: Error pushing changes: http is not defined` for `[11:59:57 PM mistress 40c1ffa] syncing repo  [11:59:57 PM mistress 40c1ffa] no changes to commit  [11:59:57 PM mistress 40c1ffa] local repo exists – fetching updates...  [11:59:57 PM mistress 40c1ffa] cloning repo...  [11:59:57 PM mistress 40c1ffa] cloneOrFetchUpdates: Error cloning repo: http is not defined  [11:59:57 PM mistress 40c1ffa] comparing local HEAD (40c1ffa) to fetched origin/mistress (eeb2b11)  [11:59:57 PM mistress 40c1ffa] local HEAD (40c1ffa) has extra commits  [11:59:57 PM mistress 40c1ffa] pushing changes to GitHub...  [11:59:57 PM mistress 40c1ffa] pushOrDivergeAndPush: Error pushing changes: http is not defined.` i added a queue vote, visited the queues page, and then went back to the main page (not any content). oh and refreshing a bunch eventually put me on a diverged branch. cool. `[12:01:22 AM mistress 40c1ffa] syncing repo  [12:01:22 AM mistress 40c1ffa] no changes to commit  [12:01:22 AM mistress 40c1ffa] local repo exists – fetching updates...  [12:01:22 AM mistress 40c1ffa] repo fetched successfully  [12:01:22 AM mistress 40c1ffa] local HEAD (40c1ffa) has extra commits  [12:01:22 AM mistress 40c1ffa] comparing local HEAD (40c1ffa) to fetched origin/mistress (36689c1)  [12:01:22 AM mistress 40c1ffa] local HEAD (40c1ffa) has extra commits  [12:01:22 AM mistress 40c1ffa] pushing changes to GitHub...  [12:01:23 AM mistress 40c1ffa] pushOrDivergeAndPush: Push rejected due to non-fast-forward. Creating session branch...  [12:01:23 AM mistress 40c1ffa] pushOrDivergeAndPush: Creating and switching to branch: mistress-session-1743055283476  [12:01:23 AM mistress-session-1743055283476 40c1ffa] pushOrDivergeAndPush: Switched to branch: mistress-session-1743055283476  [12:01:24 AM mistress-session-1743055283476 40c1ffa] pushOrDivergeAndPush: Sync complete: diverged changes are now pushed to branch mistress-session-1743055283476.  [12:01:24 AM mistress-session-1743055283476 40c1ffa] Creating PR for branch mistress-session-1743055283476...  [12:01:24 AM mistress-session-1743055283476 40c1ffa] Creating PR for branch mistress-session-1743055283476...  [12:01:25 AM mistress-session-1743055283476 40c1ffa] PR already exists for branch mistress-session-1743055283476. Trying again to retrieve it.  [12:01:25 AM mistress-session-1743055283476 40c1ffa] PR created for branch mistress-session-1743055283476.`. i don't understand why the push would reject though. somehow it becomes 2 commits ahead 10 commits behind mistress just from adding a new queue and then adding a queue vote. it's like i was on an old page that was still open and it didn't sync until after the ui operation. hmm. i wonder if there's a way to make it sync on "page is visible". probably tbh. pretty painless overall to un-diverge. just.. probably unnecessary if it could auto-merge? should probably keep it though to make that not happen as much as possible, and then remove it if it is unavoidable in some cases
- make it sync on "page is visible"
- do debounced sync on visibility and focus changes https://chatgpt.com/c/67e58cec-bb98-800c-829a-9d513192634e
- pwa offline and background sync https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Guides/Offline_and_background_operation , https://developer.mozilla.org/en-US/docs/Web/API/Web_Periodic_Background_Synchronization_API
- verify two tabs syncing at the same time cant fuck anything up (e.g. do i have to lock the indexeddb git operations)
- make tooltip and click expand in series/sequencing show note
- make add consumption ui (with note) to series/sequencing view so you can mark the handful that you just watched all watched at once
- add description to page title in addition to title and qid. `Title (Description) - ueueYOU (Qid)` maybe
- move series/sequencing "follows" and "followed by" at the top by the title
- add link to main ueue that goes to main ueue page
- fr fucking make consumptions/votes and wikidata and series/sequencing all load separately from each other. shit's so slow
- public queues? additional auth to public repos? multi auth in general? /public-repo? or something? esp for sharing your own progress or giving someone a link to check where we're at if they're watching with you. actually the github path to a public repo should be completely fine and should work patless. just need to work out rw for main user else read only or something on the repo side
- pre fetch content for followed by in the background
- name diverged branches better
- move all zip tools gpt bullshit into scratch-prototyping/dont-use-4o lol
- make theme better
- make theme changing ux to call setTheme light or dark or nothing to be sustem preference
- happy `remote already updated (skipping push)` on `[1:42:13 AM mistress cdf11c8] cloneOrFetchUpdates: Error cloning repo: http is not defined` should not be possible. both cause i should load http correctly but also because i'm not detecting clone failures
- move search into header
- routes and 404 redirect hack for cleaner api urls https://chatgpt.com/c/67f69c0b-0d70-800c-9791-415a32b7832b

phase 2
- fetching 
https://cors-proxy.m7ur7l3.workers.dev/github.com/7ur7l3org/tracking-repo/info/refs?service=git-upload-pack always gets a 401 and then a 204 options preflight and then a 200. possibly this code is bad making the 401 request idk
- "load by token write access" is returning a lot of "disk cache" responses. i feel like clicking the button should hard refresh the responses esp if they've changed something on github's end
- make it not slow
- refactor to reduce the number of lines in each file to be easier to edit with ai
- update trakt converter, re-export trakt, and fully migrate
- make wikidata updater ui for external id's that previews the batch update thing and then links to it to run from there
- make import/export repo to disk (manual for now, eventually sync to disk that syncs tar of indexeddb repo to disk (nondestructively?), but currently it's way too slow to fs directly and the access popups kinda suck -> or am i just not using the good browserfs..)
    - https://github.com/isomorphic-git/isomorphic-git/blob/main/docs/fs.md#browserfs Besides IndexedDB, BrowserFS supports many different backends with different performance characteristics (some backends support sync operations, some only async), as well as different features such as proxying a static file server as a read-only file system, mounting ZIP files as file systems, or overlaying a writeable in-memory filesystem on top of a read-only filesystem. You don't need to know all these features, but familiarizing yourself with the different options may be necessary if you hit a storage limit or performance bottleneck in the IndexedDB backend I suggested above.
- add verbose trace debug logging (that normally are filtered out of console)
- allow adding/removing consumption or queue vote from series/sequencing information view
- make pr's that don't actually have any conflict auto-merge and branch clean up. long term tho. concurrent equivalent updates should be rare
- make it so that merely visiting a thing doesn't auto-commit it to the repo probably. renderEntity probably just shouldn't ensureMediaEntry/saveBackendData. ensureMediaEntry should probably automatically saveBackendData if it changes
- make /raw or something to view the raw json
- make one click cloudflare worker button not trash
- probably change default cors proxy to https://ueue-cors-proxy.7ur.workers.dev/ linking to source and noting why https://cors.isomorphic-git.org/ doesn't work
- can easily add friends and view other people's queues and watchlists without logging out by sharing and saving to meta read-only pat's! probably via url ?repo=blah&pat= that disables sync and puts you in read only mode for their stuff! should also maybe once per page load fetch all those friends' repos too so they're always kept updated for offline
- update updateGitSyncStatus to not be based on a hardcoded colour string but based on the css class list directly (or something)
- mal supports rss, i should too. esp when making things public is real and ergonomic

---

done
-- oh the whole site broke wow https://ueue.7ur.it/?id=Q97585868 equeue
-- https://ueue.7ur.it/?id=Q653753 this is broken
-- https://ueue.7ur.it/?id=Q133252244 this is broken and never loads :/ -> no idea what broke it but probably mutex timeout that randomly stopped happening when debugged from laptop
-- make page titles be ascii-fied title and description
-- make ue<tab>search url tab search work
-- make url tab search work
-- figure out how to make `ue <tab>` take me to the "search ueue" thing -> opensearch link rel search
-- give focus to search on index.html main page load so you can directly type the search (if not doing url tab search whenever i get that working) -> yes!
-- make it default to the cursor in the search -> yes!
-- make favicon be you finger, maybe nice black and white monospace ish font -> nah just is the legit emoji!
-- make emoji favicon
-- probably move the theme logic back into theme.js now that the phone's css cache is updated by visiting it manually and spam refreshing -> this is done. also now doing some anti cache things on deploy
-- why the fuck is css broken -> deploy turns <link rel="stylesheet" href="styles.123abcd.css"> into <link rel="styles.dca3bc1.css"> amazing -> regex is hard. the deploy script is kinda really shitty
-- make all files roll to -vN or -firmmmmmmmm or -firm32m so there can be no caching on merge into deploy -> yes
-- make cloudflare-deploy branch to merge mistress into so i dont abuse their autobuilds unnecessarily -> mistress is deploy. mattress is dev
-- made update-site-dependencies.py actually do as advertized
-- make work on file:// -> `http` was loaded by isomorphic-git-1.29.0-http-web-index.umd.js as `GitHttp` so just had to use that. didn't need the es 6 http-web-index.js module itself at all
-- pat doesn't wrap on a phone (and is wider than the phone screen) causing the whole page to be too wide (zooming out one actually fixes it, but i should make it wrap or overflow into the void or something..) -> overflow auto
-- fix width/wrapping
-- dark theme working on phone (dumb phone rules https://chatgpt.com/c/67e24631-3cb4-800c-8c74-b2cffff5cb09 ) -> i don't think it was the putting js explicitly in the html above the theme.js script. i think it was literally that the phone was caching a really old version of the css super aggressively
-- dark theme
-- fix that automatic sync is broken on ueue.7ur.it but works fine on ueue.pages.dev -> aha 7ur.it is loading rocket-loader.min.js and pages.dev isn't. disabled it cause i couldn't get it to even load the script that maybe fixed it
-- find and spin up working cors proxy and make one click button for users to spin up their own personal one. for now running python script on localhost:8088 -> https://dash.cloudflare.com/d3f974b3287fb000e629aafe6423b481/workers/services/view/ueue-cors-proxy/production/deployments = https://ueue-cors-proxy.7ur.workers.dev/ = https://github.com/7ur7l3org/cors-proxy . one click button is trash
-- put on github pages -> put on cloudflare pages! https://dash.cloudflare.com/d3f974b3287fb000e629aafe6423b481/pages/view/ueue = https://ueue.7ur.it/
